{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "pnpm build",
            "group": "build",
            "label": "pnpm: build",
            "problemMatcher": ["$esbuild"]
        }
    ]
}
